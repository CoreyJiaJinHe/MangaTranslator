"""Entry point skeleton.
Currently does nothing: real initialization (config load, service wiring, UI) deferred
until post-skeleton approval.
"""
from __future__ import annotations

# Placeholder main; kept to allow `python main.py` without errors.

def main() -> None:
    """Skeleton main function.
    Future tasks:
      - Load configuration
      - Initialize registries
      - Instantiate services
      - Launch GUI / CLI flow
    """
    return  # no-op

if __name__ == "__main__":
    main()
